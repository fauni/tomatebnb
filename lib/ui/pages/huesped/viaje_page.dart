import 'package:flutter/material.dart';

class ViajePage extends StatelessWidget {
  const ViajePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Viajes'),),
    );
  }
}