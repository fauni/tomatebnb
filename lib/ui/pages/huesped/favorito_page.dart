import 'package:flutter/material.dart';

class FavoritoPage extends StatelessWidget {
  const FavoritoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Favorito Page'),
      ),
    );
  }
}